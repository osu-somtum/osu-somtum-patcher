<Window x:Class="OsuPatcher.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="osu!somtum patcher"
        Width="440" Height="440"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="OnWindowDrag">

    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="6" Background="{TemplateBinding Background}"
                                BorderBrush="#3A3A3A" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#383838"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#222222"/>
                                <Setter Property="Foreground" Value="#777777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="Background" Value="#F2645A"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="3" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#FF7B72"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Background="#1E1E1E">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Button Grid.Row="0" Style="{StaticResource CloseButton}"
                    Content="X" Width="46" Height="26"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,8,10,0" Click="OnClose"/>

            <!-- Logo -->
            <Grid Grid.Row="1" Margin="0,4,0,0">
                <Image x:Name="LogoImage" Stretch="Uniform" MaxHeight="170" Margin="28,0"
                       Source="pack://application:,,,/Assets/logo.png"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock x:Name="LogoPlaceholder" Text="osu!somtum"
                           FontSize="44" FontWeight="Bold" Foreground="#EDEDED"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="Collapsed"/>
            </Grid>

            <!-- Status -->
            <StackPanel Grid.Row="2" Margin="0,0,0,18">
                <TextBlock x:Name="StatusText" Text=""
                           Foreground="#F2645A" FontSize="14"
                           TextAlignment="Center" TextWrapping="Wrap" Margin="20,0"/>
                <TextBlock x:Name="LocateLink" TextAlignment="Center" Margin="0,2,0,0"
                           Visibility="Collapsed">
                    <Hyperlink Foreground="#F2645A" Click="OnLocate">
                        <Run Text="Locate osu!.exe"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

            <!-- Inject button -->
            <Button Grid.Row="3" x:Name="InjectButton"
                    Style="{StaticResource ActionButton}"
                    Content="Inject" Height="56" Margin="60,0,60,28"
                    Click="OnInject"/>
        </Grid>
    </Border>
</Window>
